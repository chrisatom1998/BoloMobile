# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

# Model roles

Use Fable as the orchestrator: planning, code review, delegation, and final synthesis.
Delegate implementation work to Opus as the builder — spawn it via the Agent tool with
`model: "opus"` for writing and editing code, then review its output before landing.
